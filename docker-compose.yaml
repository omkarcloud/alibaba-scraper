services:
  alibaba-scraper:
    build: .
    restart: unless-stopped
    shm_size: 1g
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # A residential proxy is strongly recommended — see the README.
      # - ALIBABA_PROXY=http://user:pass@host:port
